Hey here's a simple chakra practice file using Chakra components along with react with js
To run this you first need to run the command for json server
json-server -w ./data/db.json
then type npm run dev
